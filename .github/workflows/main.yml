name: Gerar APK - Nosso Tempo V2

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar projeto
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Mostrar versões
        run: |
          echo "Node:"
          node --version

          echo "NPM:"
          npm --version

          echo "Java:"
          java -version

      - name: Instalar dependências
        run: npm install

      - name: Criar Android
        run: |
          if [ ! -d "android" ]; then
            echo "Criando projeto Android..."
            npx cap add android
          else
            echo "Projeto Android já existe."
          fi

      - name: Sincronizar Capacitor
        run: npx cap sync android

      - name: Aplicar Nosso Tempo V2
        run: node scripts/patch-android.mjs

      - name: Dar permissão ao Gradle
        run: chmod +x android/gradlew

      - name: Mostrar versão do Gradle
        working-directory: android
        run: ./gradlew --version

      - name: Limpar build anterior
        working-directory: android
        run: ./gradlew clean

      - name: Gerar APK
        working-directory: android
        run: ./gradlew assembleDebug --stacktrace

      - name: Verificar APK
        run: |
          echo "Arquivos gerados:"
          ls -lah android/app/build/outputs/apk/debug/

      - name: Salvar APK
        uses: actions/upload-artifact@v4
        with:
          name: Nosso-Tempo-V2-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
